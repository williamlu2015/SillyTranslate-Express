/**
 * @typedef {Object} Translation
 * @property {!Entry[]} results   TODO
 * @property {?string} translationId
 */

/**
 * @typedef {Object} Entry   a step in a series of translations
 * @property {!string} langName   the language the text at this step is in
 * @property {!string} text   the text at this step
 */
